{
  "name": "aula-06",
  "version": "1.0.0",
  "private": true,
  "description": "Repo root wrapper to start the myapp express scaffold",
  "scripts": {
    "start": "node ./myapp/bin/www",
    "start:dev": "cd myapp && npm start"
  }
}
